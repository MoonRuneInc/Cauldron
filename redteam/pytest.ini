[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    rate_limit: tests that require rate limiting (will fail if absent)
    deployment: tests that require specific deployment configuration
    slow: tests that take significant time (e.g., brute force simulations)
